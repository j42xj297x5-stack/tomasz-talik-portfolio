# GitHub Workflow (Initial)

- Use focused branches and meaningful commit messages.
- Keep documentation changes in sync with code changes.
- For significant changes, produce/update an audit and (when meaningful) a snapshot.
- Do not treat docs as optional; `docs/current` is part of the source of truth.
