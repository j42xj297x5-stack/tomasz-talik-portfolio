# Frontend Runtime Model

Planned: Vite + Vanilla JavaScript modules + Three.js.

Layers:
1. Scene layer (WebGL/Three.js).
2. Interaction bridge (events/select state).
3. Overlay UI layer (HTML/CSS panels).
4. Content layer (editable text/data modules).
